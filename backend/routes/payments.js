const express = require('express')
const router = express.Router()

// Legacy Razorpay route file. These endpoints are no longer mounted by the application,
// and PhonePe integration uses /api/payment instead.
module.exports = router
